[pytest]
testpaths = tests
# Disable broken third-party pytest plugins that ship at the user's
# global site-packages level (anchorpy expects pytest_xprocess but it's
# not installed). We don't use those here.
addopts = -p no:anchorpy
